apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{values.name}}
  description: python app that displays time, named ${{ values.name }}
  # Example for optional annotations
  annotations:
     github.com/project-slug: nageshadik/${{ values.name }}
     backstage.io/techdocs-ref: dir:.
spec:
  type: service
  owner: development
  lifecycle: experimental